#!/usr/bin/env python3
"""Old speedup plotting is disabled during benchmark reconstruction."""

raise SystemExit(
    "old speedup plotting is disabled; use a finalized warmed-step or "
    "solve-to-target schema before plotting ratios"
)
